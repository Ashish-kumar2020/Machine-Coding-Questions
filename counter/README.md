## Redux Tutorial

## Basic Terminologies

### Redux
-> Redux is a state management tool, which help us in creating centeral Data store.

### Reducers
-> Manages the state and returns the newly updated state

### Actions
-> Actions have 2 properties type: which is a unique identifier and payload which has data.

### Dispatch
-> Dispatch is used to send actions to updated the data
